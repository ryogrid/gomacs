## Codebase Patterns
- Buffer is stored as `[][]rune` (Lines field), one slice per line
- Buffer tracks cursor via `CursorR`/`CursorC` fields
- Cursor movement methods on Buffer: MoveForward, MoveBackward, MoveUp, MoveDown, MoveBeginningOfLine, MoveEndOfLine
- Use `screen.ShowCursor(col, row)` to position the terminal cursor
- `NewBuffer()` creates empty buffer, `NewBufferFromFile(filename)` loads from disk
- `drawBuffer(screen, buf)` renders buffer to tcell screen, must call `screen.Show()` after
- Long lines truncated at screen width, lines beyond screen height not rendered
- Use `os.ReadFile` + `strings.Split` for file loading, trim trailing newline first
- Buffer tracks `ScrollOffset` (top visible line); use `AdjustScroll(viewHeight)` after cursor changes
- Kill ring is `[][]rune` on Buffer; `lastKill` bool tracks consecutive kills for appending
- Use `ClearLastKill()` before non-kill operations to break consecutive kill appending
- For C-k handler, call `redraw(); continue` to skip the general redraw (avoids clearing lastKill flag)
- Cursor display uses `screen.ShowCursor(buf.CursorC, buf.CursorR - buf.ScrollOffset)` to account for scroll
- `drawBuffer` renders from `buf.ScrollOffset`, mapping buffer row to screen row via `bufRow = screenRow + ScrollOffset`
- Mark/Region: `MarkR`, `MarkC`, `MarkActive` on Buffer; `SetMark()`, `DeactivateMark()`, `KillRegion()`, `CopyRegion()`
- `InRegion(row, col)` for highlight checks; region bounds are ordered via `regionBounds()` helper
- Alt key bindings use `ev.Modifiers()&tcell.ModAlt != 0` with `ev.Rune()` check; also handle Esc-prefix for terminals
- Handle both `tcell.KeyBackspace` and `tcell.KeyBackspace2` for cross-terminal compatibility
- Regular character insertion in KeyRune case goes in the `else` branch after the Alt modifier check
- Status line on row `height-2`, message area on row `height-1`; text area height = `textAreaHeight(screenHeight)` = `height - 2`
- Use `tcell.StyleDefault.Reverse(true)` for status line reverse video styling
- C-x prefix key: set `prefixCx = true`, show "C-x-" in message area, then handle second key on next event
- Message area is on row `height-1`; drawn by `drawMessageLine(screen, msg)` after `drawBuffer`
- Buffer.Save() writes to temp file then os.Rename for safe writes; clears Modified on success
- `errNoFilename` is a package-level sentinel error for buffers with no filename
- For "repeat to confirm" patterns (like force-quit), use a warning flag that resets on non-related keypresses

## 2026-03-09 - US-001
- Project scaffolding: go.mod, tcell dependency, main.go with screen init and C-c exit
- Files: go.mod, go.sum, main.go, .gitignore

---

## 2026-03-09 - US-002
- Buffer data structure with insert char, insert newline, backspace, delete char
- Files: buffer.go, buffer_test.go
- **Learnings for future iterations:**
  - Buffer uses `[][]rune` for proper Unicode support
  - All buffer mutations set `Modified = true`

---

## 2026-03-09 - US-003
- File open from CLI args and display on tcell screen
- Added `NewBufferFromFile()` to buffer.go
- Added `drawBuffer()` to main.go for rendering
- Files changed: buffer.go, main.go
- **Learnings for future iterations:**
  - `drawBuffer` clears screen, then renders visible lines/cols within screen bounds
  - File loading trims trailing newline to avoid extra empty line
  - Resize events need `screen.Sync()` then re-render

---

## 2026-03-09 - US-004
- Basic cursor movement with Emacs keybindings (C-f, C-b, C-n, C-p, C-a, C-e) and arrow keys
- Added 6 movement methods to Buffer: MoveForward, MoveBackward, MoveUp, MoveDown, MoveBeginningOfLine, MoveEndOfLine
- Cursor wraps between lines on forward/backward; clamps column when moving up/down to shorter lines
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - `screen.ShowCursor(col, row)` must be called after every redraw to position the hardware cursor
  - Cursor movement methods live on Buffer since they depend on buffer state (line lengths, bounds)
  - When moving up/down, cursor column must be clamped to the target line's length

---

## 2026-03-09 - US-006
- Wired text insertion and editing keys into the event loop
- Printable characters insert via InsertChar, Enter via InsertNewline, Backspace via Backspace, C-d via DeleteChar
- All buffer methods already existed from US-002; only main.go event loop needed changes
- Files changed: main.go
- **Learnings for future iterations:**
  - Buffer editing methods were pre-built in US-002, so wiring up editing keys is just adding cases to the event loop switch
  - KeyBackspace and KeyBackspace2 both need to be handled (different terminals send different codes)
  - Regular character insertion goes in the `else` branch of the Alt modifier check in the KeyRune case

---

## 2026-03-09 - US-005
- Scrolling and viewport: ScrollOffset tracked in Buffer, AdjustScroll keeps cursor visible
- ScrollDown/ScrollUp for page scrolling (C-v / M-v), MoveBeginningOfBuffer/MoveEndOfBuffer (M-< / M->)
- drawBuffer renders from ScrollOffset; cursor display adjusted by offset
- Esc-prefix handling for terminals that don't send ModAlt
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - Screen cursor position must subtract ScrollOffset: `ShowCursor(col, cursorR - scrollOffset)`
  - Alt/Meta keybindings need dual handling: ModAlt flag and Esc-prefix sequence
  - Page scroll moves cursor into viewport if it would be off-screen

---

## 2026-03-09 - US-007
- Status line displayed on second-to-last row with reverse video
- Shows filename (or [No Name]), [Modified] indicator, and Line X, Col Y position
- Text area height reduced to `height - 2` to reserve status line and message area rows
- Added `textAreaHeight()` helper; all viewHeight calculations now use it
- Files changed: main.go
- **Learnings for future iterations:**
  - `textAreaHeight(screenHeight)` returns `screenHeight - 2` (status + message area)
  - Status line uses `tcell.StyleDefault.Reverse(true)` for distinct background
  - Left side shows filename + modified state; right side shows cursor position
  - Cursor position displayed as 1-indexed (Line 1, Col 1) for user-friendliness

---

## 2026-03-09 - US-008
- File save with C-x C-s prefix key handling
- Added Buffer.Save() method with safe write (temp file + rename)
- Added C-x prefix key state machine in event loop
- Added message area display (drawMessageLine) on last row
- Refactored redraw into a closure to reduce duplication
- Files changed: buffer.go, main.go
- **Learnings for future iterations:**
  - C-x prefix works by setting a bool flag; next key event checks the flag and handles the second key
  - Show "C-x-" in message area while waiting for second key to give user feedback
  - Use `os.CreateTemp` in same directory + `os.Rename` for atomic-ish file save
  - Message is cleared on next keypress by resetting the message string
  - `errNoFilename` sentinel used to distinguish "no file" from actual write errors
  - The `redraw` closure captures screen/buf/message/viewHeight to reduce repeated code

---

---

## 2026-03-09 - US-009
- Quit with C-x C-c: added KeyCtrlC case in C-x prefix handler
- If buffer is modified, shows warning and sets quitWarned flag; second C-x C-c forces quit
- Removed standalone C-c quit (quit only via C-x C-c now)
- quitWarned resets on any keypress that isn't part of C-x sequence
- Files changed: main.go
- **Learnings for future iterations:**
  - "Repeat to confirm" pattern: set a bool flag on first attempt, check it on second attempt, reset on unrelated keys
  - Reset confirmation flags early in the event loop, before the C-x prefix check, to avoid stale state

---

## 2026-03-09 - US-010
- Kill and Yank: C-k kills from cursor to end of line, C-y yanks last killed text
- Kill ring stored as `[][]rune` on Buffer; consecutive C-k appends to same entry
- C-k on empty line kills the newline (joins with next line)
- Yank handles multi-line content by inserting newlines via InsertNewline
- Added `lastKill` bool for consecutive kill tracking; `ClearLastKill()` resets it
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - C-k handler uses `redraw(); continue` to skip the general ClearLastKill logic at the bottom of the event loop
  - ClearLastKill must be called early in event handling for all non-kill keys
  - Kill ring entries include newline runes ('\n') when killing across lines
  - Yank iterates over runes, calling InsertNewline for '\n' and InsertChar for others

---

## 2026-03-09 - US-011
- Mark and Region: C-SPC sets mark, C-w kills region, M-w copies region, C-g deactivates mark
- Region visually highlighted with reverse video in drawBuffer
- Added Buffer fields: MarkR, MarkC, MarkActive
- Added methods: SetMark, DeactivateMark, KillRegion, CopyRegion, RegionText, InRegion, deleteRegion, regionBounds
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - Region bounds use a helper `regionBounds()` that orders mark/point so start <= end
  - `InRegion` treats end position as exclusive (matches Emacs behavior)
  - KillRegion/CopyRegion both deactivate the mark after operation
  - C-SPC maps to `tcell.KeyCtrlSpace` in tcell
  - C-g mapped to `tcell.KeyCtrlG` — useful as a general cancel key
- Undo uses full buffer snapshots (undoEntry with Lines + cursor); max 100 entries
- `SaveUndo()` is called in the event loop before each editing command, not inside buffer methods (keeps compound ops like Yank as single undo)
- C-/ and C-_ both map to `tcell.KeyCtrlUnderscore` (same terminal code 0x1F)

---

## 2026-03-09 - US-012
- Undo: C-/ (C-_) undoes the last editing operation, supports 100+ levels
- Added undoEntry struct with Lines snapshot + cursor position
- Added SaveUndo() and Undo() methods on Buffer
- SaveUndo() called in event loop before each editing command (InsertChar, InsertNewline, Backspace, DeleteChar, KillLine, KillRegion, Yank)
- Added 7 unit tests covering insert, newline, backspace, multi-level, empty stack, kill line, and max entries
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - Search mode: `searchMode` bool in event loop; search keys handled before normal keys, other keys exit search and re-post via `screen.PostEvent(ev)`
- `SearchForward`/`SearchBackward` on Buffer with wrap-around; `indexRunes`/`lastIndexRunes` helpers
- Search highlight uses `searchHighlight` struct passed to `drawBufferWithSearch`
- C-/ and C-_ produce the same terminal code (0x1F), handled by `tcell.KeyCtrlUnderscore`
  - SaveUndo should be called at the command level in the event loop, not inside individual buffer methods, so compound operations (like Yank which calls InsertChar/InsertNewline) are a single undo entry
  - Full buffer snapshots (deep copy of Lines) are simple and correct; for large files a more efficient approach would be needed

---

## 2026-03-09 - US-013
- Incremental Search: C-s starts forward search, C-r starts backward search
- Added SearchForward/SearchBackward methods on Buffer with wrap-around
- Added indexRunes/lastIndexRunes helper functions for rune-based substring search
- Search mode in event loop: typing updates query and jumps to match, C-s/C-r cycle matches
- Enter exits search at current position, C-g cancels and restores original position
- Backspace removes last query char and re-searches from original position
- Current match highlighted with reverse video via searchHighlight struct
- Search query shown in message area ("I-search: ..." or "Failing I-search: ...")
- Non-search keys exit search mode and are re-posted via screen.PostEvent for normal handling
- Added 10 unit tests for SearchForward and SearchBackward
- Files changed: buffer.go, buffer_test.go, main.go
- **Learnings for future iterations:**
  - Search mode intercepts keys before normal event handling; use `continue` to skip normal processing
  - Use `screen.PostEvent(ev)` to re-post unhandled keys when exiting search mode so they get processed normally
  - Wrap-around search iterates all lines starting from current position, using modular arithmetic
  - `drawBufferWithSearch` extends `drawBuffer` with optional match highlighting via a struct parameter

---
