## Codebase Patterns
- Module name is `goomacs`, so term package imports as `goomacs/term`
- main.go uses term.Screen interface with term.KeyEvent, term.ResizeEvent, term.Style types
- Buffer struct holds Lines, CursorR/C, ScrollOffset, Modified, Filename, KillRing, Mark, UndoStack
- Event loop in main.go: PollEvent → dispatch by mode (search/C-x prefix/normal) → redraw
- cell struct in terminal.go: {ch rune, style Style} — used for diff-based rendering in Show()
- showForTest() in terminal_test.go uses writeStyledCell() helper — Show() and showForTest() share rendering logic
- Style is now a struct (fg Color, bg Color, reverse bool, bold bool); StyleDefault is a var, not const
- writeStyledCell() on Terminal emits combined ANSI sequence: bold, fg 256-color, bg 256-color, reverse in one \033[...m block, then resets with \033[0m
- Highlighter in highlight.go: NewHighlighter(filename) → nil if no lexer; Highlight(lines) caches [][]term.Style; StyleAt(row,col) returns cached style
- Chroma v2 API: lexers.Match(), chroma.Coalesce(), styles.Get(), lexer.Tokenise(), iter.Tokens(), style.Get(tok.Type)
- drawWindowContent(screen, win, sh) renders buffer content in window's row range
- Tab expansion handled in rendering (bufColToVisualCol)
- `buf` is a local alias for `buffers[activeBufferIdx]`
- Window struct: Buffer, ScrollOffset, StartRow, Height; ViewHeight() = Height-1

## Progress: Syntax Highlighting

## 2026-03-10 - US-001
- Changed Style from uint8 to struct with fg (Color), bg (Color), reverse (bool), bold (bool)
- Added Color type (int16) with ColorDefault = -1
- Added Foreground(), Background(), Bold() builder methods and Fg(), Bg(), IsBold() accessors
- StyleDefault is now a var (not const) since structs can't be const in Go
- Files changed: term/screen.go, term/terminal_test.go
- **Learnings for future iterations:**
  - Style is a value type (struct) — Go struct == comparison works for all comparable fields, so cell diff in Show() still works
  - StyleDefault changed from const to var — all existing call sites (main.go, tests) work without changes
  - showForTest() in terminal_test.go duplicates Show() rendering logic — when updating Show() rendering (US-002), must also update showForTest()
  - Test format strings using %d for Style need to change to %v since it's now a struct
---

## 2026-03-10 - US-003
- Created highlight.go with Highlighter struct, NewHighlighter(), Highlight(), StyleAt()
- Added chroma/v2 dependency (v2.23.1) via go get
- Implemented manual RGB→256-color conversion (rgbTo256, colorCubeIndex) since chroma v2 Colour lacks Nearest256()
- Uses chroma.Coalesce(lexer) for token merging, styles.Get("monokai") for theme
- Files changed: highlight.go, go.mod, go.sum
- **Learnings for future iterations:**
  - Chroma v2 Colour has no Nearest256() method — must manually convert via RGB→6x6x6 cube + grayscale ramp
  - Chroma Colour.IsSet() checks if colour is non-zero; Red()/Green()/Blue() extract components
  - chroma.StyleEntry.Bold is a tristate (Yes/No/Pass) — check == chroma.Yes, not just bool
  - lexers.Match(filename) returns nil for unknown extensions — caller must handle nil
---

## 2026-03-10 - US-002
- Updated Show() and showForTest() to emit 256-color ANSI sequences for fg, bg, bold, and reverse
- Extracted writeStyledCell() helper method on Terminal to share rendering logic between Show() and showForTest()
- ANSI attributes emitted in a single combined \033[...m sequence (bold;fg;bg;reverse), followed by \033[0m reset
- Default-styled cells emit no escape sequences beyond cursor positioning
- Files changed: term/terminal.go, term/terminal_test.go
- **Learnings for future iterations:**
  - writeStyledCell() is the single source of truth for style→ANSI conversion — update it for any new style attributes
  - showForTest() now calls writeStyledCell() instead of duplicating logic — no more sync issues
  - Tests verify individual attributes (fg, bg, bold, reverse) and combined attributes in one sequence
---

## 2026-03-10 - US-004
- Added HighlightDirty bool field to Buffer struct
- Set HighlightDirty = true in NewBuffer(), NewBufferFromFile(), and all edit methods: InsertChar, InsertNewline, Backspace, DeleteChar, KillLine, deleteRegion (used by KillRegion), Undo
- Yank calls InsertChar/InsertNewline which already set the flag, so no separate change needed
- Files changed: buffer.go
- **Learnings for future iterations:**
  - Edit methods that modify Lines have `b.Modified = true` — HighlightDirty follows the same pattern, set right after Modified
  - Backspace and DeleteChar each have two code paths (within-line and cross-line) that both need the flag
  - deleteRegion() is the shared implementation for KillRegion — setting the flag there covers KillRegion without needing to set it in KillRegion() itself
---

## 2026-03-10 - US-005
- Added Highlight *Highlighter field to Buffer struct
- NewBufferFromFile() creates and attaches Highlighter via NewHighlighter(filename)
- C-x C-f (find-file) also attaches Highlighter for new files
- drawWindowContent() checks HighlightDirty, re-highlights if needed, uses StyleAt() for base style
- Region/search reverse video applied on top of syntax highlighting styles
- Special buffers (*scratch*, *Buffer List*) have nil Highlighter — render normally
- Updated README.md: "Zero dependencies" → "Minimal dependencies", added syntax highlighting to features
- Files changed: buffer.go, main.go, README.md
- **Learnings for future iterations:**
  - Highlighter integration is minimal: add field to Buffer, call Highlight() in drawWindowContent, use StyleAt() for base style
  - HighlightDirty flag prevents re-tokenizing on every redraw — set false after Highlight() call
  - Reverse video for region/search is applied on top of syntax style via style.Reverse(true), preserving colors
  - NewBuffer() for special buffers (*scratch*) doesn't need a Highlighter — nil is handled gracefully
---

